# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# PyInstaller build artifacts
dist/
build/

# sde_base.db IS tracked in git (uploaded to GitHub Releases for first-run setup)
# !sde_base.db  ← override needed because *.db is ignored above

# Databáze a citlivá data (sde_base.db je výjimka — trackován v gitu)
*.db
*.db-shm
*.db-wal
!sde_base.db
data/tokens.json

# EVE SDE data (velké soubory, stahují se separátně)
data/sde.zip
data/fsd/
data/fsd_backup/

# Android / Gradle (build/ výše už pokrývá android-poc/app/build/)
.gradle/
android-poc/local.properties
android-poc/.gradle/
# build-time kopie (vyrábí prepare.sh z rootu repa — nejsou zdroj)
android-poc/app/src/main/python/app/
android-poc/app/src/main/assets/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Privátní Claude kontext (symlink na repo eve-retroindustry-notes) — nikdy do public
/CLAUDE.md
